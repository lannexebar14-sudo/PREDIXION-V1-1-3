import GenericPage from "../components/GenericPage";

export default function Page() {
  return <GenericPage title='MES PARIS' subtitle='Retrouve tous tes paris en cours.' cards=[{'title': 'Paris actifs', 'text': 'Tes paris confirmés apparaîtront ici.', 'href': '/historique', 'button': 'VOIR L’HISTORIQUE'}, {'title': 'Nouveau pari', 'text': 'Retourne sur les matchs du jour pour placer un pari.', 'href': '/sports', 'button': 'PARIER'}] />;
}
